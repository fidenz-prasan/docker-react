sudo: required
language: generic
services:
  - docker

before_install: 
  - docker build -t nimanthaprasan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nimanthaprasan/docker-react npm run test -- --coverage